module nexus_registry::era;

//! Interface for [`nexus_registry::era`].
//!
//! Calls resolve to the published package.

/// Version one Registry package witness.
public struct V1() has copy, drop, store;
